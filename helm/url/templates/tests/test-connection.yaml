apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "url.fullname" . }}-test-connection"
  labels:
    {{- include "url.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "url.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
